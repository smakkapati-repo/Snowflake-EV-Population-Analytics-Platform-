name: 'ev_pipeline'
version: '1.0.0'
config-version: 2

profile: 'ev_pipeline'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  ev_pipeline:
    +database: EV_PIPELINE
    +materialized: table
